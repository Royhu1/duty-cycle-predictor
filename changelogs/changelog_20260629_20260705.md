# Changelog — 2026-06-29 to 2026-07-05

## Q: Systematically restructure the project's directory layout and documentation to follow the JOLT_Report construction philosophy. Commit a safety checkpoint first, then self-check and test that everything still works.

**Task**: Adopt the JOLT_Report methodology — one small versioned core package under `src/`, surrounded by
gitignored workspaces; README-as-single-source-of-truth at every level; `.claude/` as committed first-class config
with rules `@import`-ed into `CLAUDE.md`; "commit only code + documentation" `.gitignore`; SemVer on the core only.
Keep the existing top-level folder names (philosophy in-place) so the active ITSC2026 paper's paths do not break.

**Result**:
- Committed a safety checkpoint (`cc149be` on `master`) capturing the prior working state; did the refactor on the
  `refactor/jolt-structure` branch.
- **Package → src-layout**: moved `dcpredictor/` → `src/dcpredictor/`. Updated `pyproject.toml`: static
  `version = "0.1.3"` (dropped `dynamic`/`setuptools_scm`), `packages.find where=["src"]`,
  `isort known_first_party = ["dcpredictor"]`. Removed the redundant `setup.py`.
- **Version reconciliation**: `version.py` now reads `importlib.metadata` with a `_FALLBACK_VERSION`; promoted the
  private default-param loaders to public `load_default_vehicle_params()` / `load_default_driving_behavior()` and
  exported them (the previously documented quickstart did not actually run).
- **`.gitignore`**: reworked to the "commit only code + docs" philosophy; un-ignored `.claude` shared config
  (`rules/`, `commands/`, `settings.json`) while keeping `settings.local.json`, runtime dirs, vendored skills, and
  `.mcp.json` ignored; added `archive/*`, `*.pptx`, `CLAUDE.local.md`. Added `.env.example`.
- **Docs**: created `.claude/rules/{code-style,naming,git-workflow,housekeeping}.md` + `.claude/README.md`; rewrote
  `CLAUDE.md` to `@import` the README and the rules (removing stale `src/duty_cycle_prediction/` + `scripts/`
  references). Rewrote the root `README.md` as the project map with corrected, runnable API examples. Added
  `src/dcpredictor/README.md` (package architecture) and per-directory READMEs for `data/`, `notebooks/`,
  `examples/`, `tests/`, `ITSC2026/`, `Transit_project/`.
- **Tests fixed** to the current API (`dcpredictor.generators`, `dcpredictor.utils.lvd`, data-model round-trips);
  also corrected a wrong expected value carried over from the old, never-run test. 16 tests pass.
- **Examples fixed**: `basic_usage.py` rewritten as an offline, key-free demo; added `predict_end_to_end.py`;
  archived the obsolete `map_visualization_example.py` (relied on removed map utilities).
- **Tidied**: moved `画图/`, `my_packages.txt`, the orphaned `src/srf_logger_data_downloader.py`, and the
  deprecated notebook into `archive/` (kept on disk, untracked).

**Verification**: `pip install -e .` succeeds under conda env `dcp`; `pytest` → 16 passed; `examples/basic_usage.py`
runs offline; `predict_end_to_end.py` byte-compiles; `.gitignore` include/exclude confirmed via `git check-ignore`.

**Version**: `dcpredictor` 0.1.3 (unchanged — refactor only, no behaviour change to the prediction logic).

## Q: Generate v1.4 of the ITSC2026 ERS presentation from v1.3, applying supervisor/colleague feedback.

**Task**: Apply review feedback to `ITSC2026/20260704-ERS-dcp_v1.3.pptx` → `_v1.4.pptx`: strengthen the key
motivation sentence; merge "Existing methods" into Motivation; retitle the framework slide "Proposed Framework";
make the Outline correspond 1:1 with the section slide titles; simplify the Route Acquisition slide; reduce the
Driving Cycle Prediction slide to the flowchart + ≤3 bullets with parameters moved to a backup slide after
Thank You; move Predicted-vs-Measured after Data Collection; unify body/caption fonts to ≥18 pt; typeset
variables as formulas (italic letter + subscript).

**Result**: Built `ITSC2026/20260704-ERS-dcp_v1.4.pptx` (19 slides) programmatically with `python-pptx`
(scripted, reproducible edits from v1.3):
- Motivation: "Driving cycle is the factor governing energy consumption" (the reviewer's stronger option);
  existing-methods bullet polished ("synthetic", em-dash construction). Outline reduced to 8 items matching the
  section slides; slide 4 retitled "Proposed Framework".
- Route Acquisition: deleted the dense offset-diagram group (and the slide's `<p:timing>` animations that
  referenced it), simplified the API return snippet, enlarged fonts to 16–18 pt, dark text on the light code box.
- Driving Cycle Prediction: flowchart + 3 bullets only; parameter table, fixed-parameter notes and look-ahead
  equations moved to a new "Backup: Driving Cycle Model Parameters" slide after Thank You.
- Predicted-vs-Measured retitled "Validation: …" and moved after Data Collection; captions/sub-bullets bumped to
  ≥18 pt across content slides; Latin variable letters italicised, existing subscript formatting preserved.
- Deployment slide simplified to ≥18 pt throughout: 6-line API request/response snippet, three parameter→effect
  mappings, and a rebuilt 4-column tier table (L1 SIMPLE / L2 route physics / L3 this work); dropped the
  fine-print footer (base URL, auth, rate limit — say verbally if asked).

**Verification**: PowerPoint COM opens the file (a first build failed to open — root-caused to dangling animation
`spTgt` references after deleting the diagram group; fixed by removing the slide's timing node). All edited
slides exported to PNG and visually checked, including a regression where re-formatting already-subscripted runs
changed line wrapping and broke the η glyph rendering on the LVD slide (fixed: leave pre-existing subscripts
untouched).
