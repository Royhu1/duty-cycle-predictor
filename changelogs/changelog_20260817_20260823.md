# Changelog — 2026-08-17 to 2026-08-23

## Q: Initialise this folder as the dedicated duty-cycle-predictor repository, migrating the core from the old workspace and committing a first version to a remote.

**Task**: Split the predictor out of the former mixed workspace
(`Research/duty-cycle-predictor-workspace`) into this clean, self-contained repository. Scope it strictly to the
**development and testing of the predictor** so that others can clone it to use, experiment with, and test the
toolkit. Exclude project-level work: the ITSC 2026 paper (already moved to
`Research/MEETING-CONFERENCE/ITSC-2026-09`), the `Transit_project/` fleet analysis, and reference PDFs (`ref/`).

**Result**:
- **Migrated (copied — the old workspace is left untouched as a fallback)**: `src/dcpredictor` (core package,
  v0.1.3), `tests/`, `examples/`, `notebooks/`, `data/` (gitignored raw GPS trajectories), `changelogs/`,
  `.claude/` shared config (README + rules), and the root files (`README.md`, `CLAUDE.md`, `AGENTS.md`,
  `pyproject.toml`, `requirements*.txt`, `.gitignore`, `.env.example`).
- **Excluded**: `Transit_project/` (1.1 GB, independent analysis), `ref/`, the old `archive/`, all generated
  artefacts (`results/`, `htmlcov/`, `.coverage`, caches), and the old git history (checkpoint/paper commits) —
  this repo starts with a fresh, clean history; the full history remains in the old workspace's `.git`.
- **Notebook hygiene**: cleared the bulky cell outputs of the four multi-MB run notebooks
  (`run_mult_test`, `run_single_test`, `run_single_test_reportonly`, `run_single_test_for_srf2025annualmeeting`),
  shrinking them from ~20 MB to ~170 KB per the "clear bulky outputs before committing" convention.
- **Docs rescoped**: removed all `ITSC2026/` / `Transit_project/` / `ref/` references from `README.md`,
  `CLAUDE.md`, `.claude/rules/*`, `data/README.md` and `notebooks/README.md`; added a repository-purpose statement
  (predictor development & testing only; papers and analyses live in separate repos that install `dcpredictor`).
- **Housekeeping zones**: added `tmp/` (gitignored) and fresh `archive/` (`.gitkeep`) and `results/` zones;
  `.gitignore` cleaned of ITSC-specific rules. Refreshed the generated global-conventions block in `AGENTS.md`.
- **Git**: initialised a new repository on `main` and pushed the initial commit to a new private GitHub remote.

**Verification**: `pip install -e .` + `pytest` under conda env `dcp` (see the commit for the test result);
`git status` clean after the initial commit; tracked file list matches the old repo's core set minus the excluded
workspaces.

**Version**: `dcpredictor` 0.1.3 (unchanged — repository migration only, no code change).
